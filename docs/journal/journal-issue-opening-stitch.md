# Future issue opening notes

## 2026-04-12

- User asked to implement the previously proposed plan for future implementation issues.
- Current repository context:
  - repo: `cgm-16/weatherpane`
  - branch: `main...origin/main`
  - no open issues
  - no open PRs
- Previously completed task issues exist through `WP-009`; corresponding implementation PRs are closed.
- Separate design-token issue `#25` and PR `#26` already landed; future theme/chrome work must treat that as prior context, not duplicate it.
- Execute staged opening only for `WP-010` through `WP-016`.
- Add a `Design handoff / Stitch MCP` section to every screen- or UI-state-heavy issue so implementers fetch or generate visual specs/prototypes before coding.
- Defer `WP-017` through `WP-022` until the earlier product path lands, matching `docs/taskmap.md`.
